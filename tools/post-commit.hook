#!/bin/sh
# Notion sync — appended to .husky/post-commit
# Safe: exits 0 on any failure, never blocks commits.

if [ -z "$NOTION_INTEGRATION_TOKEN" ]; then
  exit 0
fi

command -v python >/dev/null 2>&1 || exit 0

python tools/notion_sync.py 2>&1 || true
