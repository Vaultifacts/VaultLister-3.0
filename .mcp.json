{
  "mcpServers": {
    "github": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "[CONFIGURE]"
      }
    },
    "filesystem": {
      "command": "cmd",
      "args": [
        "/c",
        "npx",
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "."
      ]
    },
    "openclaw-memory": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@openclaw/mcp-memory"],
      "env": {
        "MEMORY_FILE": ".openclaw/memory/context.json"
      }
    }
  }
}
