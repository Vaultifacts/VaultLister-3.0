{
  "name": "deploy",
  "description": "Trigger a VaultLister 3.0 Docker deployment",
  "trigger": "deploy",
  "input_schema": {
    "type": "object",
    "properties": {
      "confirm": {
        "type": "boolean",
        "description": "Must be true to proceed with deployment"
      }
    },
    "required": ["confirm"]
  },
  "claude_skill": "/deploy",
  "requires_confirmation": true,
  "notify_on_complete": true
}
