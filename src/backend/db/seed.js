// Seed script — called by `bun run db:reset` after init.js
// Seeds are already applied inside initializeDatabase() (helpContent, demoData,
// brandSizeGuides), so this file is intentionally a no-op stub.
console.log('✓ Seeds applied during db:init — nothing extra to do here');
