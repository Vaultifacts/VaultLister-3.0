{
  "deny_patterns": 28,
  "validate_bash_patterns": 0,
  "protected_files": 0,
  "agent_files": 14,
  "memory_rules": 36,
  "_comment": "Update this file whenever any of the counted sources change. Run scripts/reconcile-notion.sh to verify counts. See Notion Rules Architecture page for documentation.",
  "_sources": {
    "deny_patterns": ".claude/settings.json — grep -c '\"Bash(' (lines in deny array)",
    "validate_bash_patterns": ".claude/hooks/validate-bash.sh — grep -c '^  \"' (BLOCKED_PATTERNS array entries)",
    "protected_files": ".claude/hooks/protect-files.sh — grep -c '^  \"' (PROTECTED_FILES array entries)",
    "agent_files": ".claude/agents/*.md — file count",
    "memory_rules": "~/.claude/projects/C--Users-Matt1-OneDrive-Desktop-Claude-Code-Project-Brainstormer-vaultlister-3/memory/ — file count excluding MEMORY.md"
  }
}
